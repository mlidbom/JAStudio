{
  "_comment": "C# LSP workaround for anthropics/claude-code#16360. See CLAUDE.workarounds.md → 'C# language server' for the full story. Once the bug is fixed, delete the `env` block; `lspSettings` is currently a no-op but should take over.",
  "lspSettings": {
    "csharp": { "solutionPathOverride": "src/src_dotnet/JAStudio.slnx" }
  },
  "env": {
    "CSHARP_LSP_SOLUTION_REL": "src/src_dotnet/JAStudio.slnx"
  },
  "_comment_plugins": "prerequisite: pyright-langserver on PATH.",
  "enabledPlugins": {
    "pyright-lsp@claude-plugins-official": true
  },
  "_comment_hooks": "Fail loudly if the .claude-shared symlinks degraded to plain text files (a checkout without core.symlinks / Windows Developer Mode). The verifier lives in the shared catalog; see .claude-shared/README.md.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "bash \"${CLAUDE_PROJECT_DIR}/.claude-shared/git-scripts/verify-claude-symlinks.sessionstart-hook.sh\"" }
        ]
      }
    ]
  }
}
