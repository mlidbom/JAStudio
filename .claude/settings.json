{
  "_comment": "C# LSP workaround for anthropics/claude-code#16360. See CLAUDE.md → 'C# language server' for the full story. Once the bug is fixed, delete the `env` block; `lspSettings` is currently a no-op but should take over.",
  "lspSettings": {
    "csharp": { "solutionPathOverride": "src/src_dotnet/JAStudio.slnx" }
  },
  "env": {
    "CSHARP_LSP_SOLUTION_REL": "src/src_dotnet/JAStudio.slnx"
  },
  "_comment_plugins": "prerequisite: pyright-langserver on PATH.",
  "enabledPlugins": {
    "pyright-lsp@claude-plugins-official": true
  }
}
